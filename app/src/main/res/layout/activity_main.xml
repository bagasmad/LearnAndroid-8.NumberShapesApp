<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

<EditText
    android:layout_width="200dp"
    android:layout_height="wrap_content"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintTop_toBottomOf="@+id/textTitle"
    android:layout_marginTop="20dp"
    android:hint="Insert number"
    android:id="@+id/insertNumber"
    android:textAlignment="center"
    android:textSize="25sp"
    android:inputType="number"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@+id/insertNumber"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:id="@+id/defineButton"
        android:onClick="buttonClick"
        android:text="Define Shape"
        android:layout_marginTop="16dp"/>
    <TextView
        android:layout_marginTop="64sp"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:text="This program will define if number inserted is a triangular or a square number"
        android:textSize="16dp"
        android:textAlignment="center"
        android:id="@+id/textTitle" />
</androidx.constraintlayout.widget.ConstraintLayout>